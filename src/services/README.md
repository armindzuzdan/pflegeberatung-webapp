# Services

Zielordner fuer wiederverwendbare Service-Module, zum Beispiel LocalStorage-Zugriffe oder Export-Adapter.

Die aktuelle Datenlogik liegt noch in `src/App.jsx`, damit die bestehende Funktionalitaet erhalten bleibt.
